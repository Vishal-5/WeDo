# Quick Fix - Run This!

Open terminal in `frontend` folder and run:

```bash
npm run fix-icons
```

That's it! All PWA icons will be generated and install will work perfectly.

Then deploy to HTTPS and users can click "Install App" button to download your app!
